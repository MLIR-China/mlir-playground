name: Deploy main branch to production

on:
  workflow_dispatch:

concurrency:
  group: deploy-prod
  cancel-in-progress: true

jobs:
  deploy:
    uses: ./.github/workflows/deploy-site.yml
    with:
      deploy_message: "Deploy main at ${{ env.COMMIT_SHA }}"
    secrets: inherit
